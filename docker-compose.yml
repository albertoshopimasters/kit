version: "3.9"

services:
  kit-app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: kit-app
    ports:
      - "3000:3000"
    env_file:
      - .env.dev
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN}
    restart: unless-stopped
