{% assign section_header = 'customization.cart.title' | t %}

<div class="kit-section">
    {% comment %} This section header can be removed if you prefer to have the subtotal handled separately {% endcomment %}
    {% render 'component-section-header', section_title: section_header, price_value: '132,98€' %}
    <div>
        <button class="add-to-cart-button">
            {{ 'customization.cart.button.title' | t | upcase }}
        </button>
    </div>
</div>
